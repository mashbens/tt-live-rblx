fastapi>=0.110
uvicorn>=0.27
httpx>=0.27
# Dipakai tiktok_listener.py buat baca komentar TikTok Live.
TikTokLive>=6.4.0
python-dotenv>=1.0.0

# Lua sungguhan, cuma dipakai `make test-podium` buat menguji
# pembukuan slot podium di my_sscript_lua tanpa membuka Studio.
lupa>=2.8
